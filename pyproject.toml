[project]
name = "mcp-study"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Tatsuya Takamura", email = "tkmr2000+gh@gmail.com" }
]
requires-python = ">=3.10.4"
dependencies = [
    "mcp[cli]>=1.6.0",
]

[project.scripts]
mcp-study = "mcp_study:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "pytest>=8.3.5",
    "ruff>=0.11.7",
]
